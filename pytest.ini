[pytest]
markers =
    unit: fast unit tests without network
    integration: integration tests with external services
    analysis: tests for analysis layer
    statistics: tests for statistics layer